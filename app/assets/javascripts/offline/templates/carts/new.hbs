<section>
  <h1>Novo pedido</h1>

  <div class="item_search"></div>

  {{outlet inventoryItemSearch}}

  <br />
  <br />
  <h2>{{itemsQuantityHeadline}}</h2>

  {{render "items" controller.cartItems controller='cartItems'}}
</section>

<section>
  <h2>Cliente</h2>
  {{outlet cartCustomer}}
</section>

{{#if isOrderReadyToBePlaced}}
  <section>
    <a href="#" class="place_order_button" title="place_order" {{action "placeOrder"}}>&nbsp;</a>
  </section>
{{/if}}

{{#if isOrderPlaced}}
  <section>
    <div class="order_status order_placed">
      Pedido concluído com sucesso!
    </div>
  </section>
{{/if}}
