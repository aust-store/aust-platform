<%= view_header(back_to: admin_banners_path) %>

<header>
  <h1><%= t('admin.banners.edit.title') %></h1>
</header>

<div class="main_container">
  <div class="form">
    <%= render 'form' %>
  </div>
</div>
