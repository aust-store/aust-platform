<%= view_header(back_to: admin_customers_path) %>

<header>
  <h1><%= t('admin.customers.new.title') %></h1>
</header>

<div class="main_container">
  <div class="form">
    <%= render 'form' %>
  </div>
</div>
