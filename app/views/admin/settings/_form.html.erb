<%= simple_form_for @settings, url: admin_settings_path, remote: true do |s| %>
  <div class="column_pane_half left">
    <div class="section shipping">
      <h2><%= t ".shipping.title" %></h2>
      <%= s.input :zipcode %>
    </div>

    <%= s.submit %>
  </div>
<% end %>
