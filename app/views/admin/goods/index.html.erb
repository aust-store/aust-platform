<%= render "layouts/admin/nav_history" %>

<header>
  <h1>Estoque</h1>
</header>

<div class="main_container">
  <table class="listing">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Referência</th>
        <th>Total em estoque</th>
      </tr>
    </thead>
    <tbody>
  <% if @goods %>
    <% @goods.each do |good| %>
      <tr class="<%= cycle('odd', 'even') %>">
        <td><%= link_to good.name, admin_inventory_good_path(good) %></td>
        <td><%= good.reference %></td>
        <td><%= good.last_balance.try(:total_quantity) %></td>
      </tr>
    <% end %>
  <% end %>
    </tbody>
  </table>

  <br />

  <%= small_button new_good_or_balance_admin_inventory_goods_path, :text => "Adicionar item", :image => 'admin/icons/plus_16.png' %>
</div>
