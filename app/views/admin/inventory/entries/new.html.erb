<%= render partial: "layouts/admin/nav_history",
           locals: { 
             back_to: admin_inventory_good_entries_path(@good),
             text: "Voltar às entradas no estoque de #{@good.name}"
           } %>

<header>
  <h1><%= @entry.good.name %></h1>
  <p>Nova entrada no estoque</p>
</header>

<div class="main_container">

  <div class="form">
    <%= render 'form' %>
  </div>

</div>
