<%= simple_form_for([:admin, @item]) do |f| %>

  <%= f.input :name,
        input_html: { id: "inventory_item_name" } %>

  <%= f.input :description %>

  <%= f.input :merchandising %>

  <%= f.input :reference %>

  <%= f.submit %>
  <%= t("admin.generic.or") %>
  <%= link_to t("admin.generic.cancel"), "javascript: history.back()" %>
<% end %>
