<%= render partial: "layouts/admin/nav_history",
           locals: {
             back_to: admin_inventory_items_path,
             text: "Voltar à listagem de itens"
           } %>

<header>
  <h1>Crie um novo item</h1>
</header>

<div class="main_container">
  <div class="new_item_or_entries form">

    <%= simple_form_for([:admin, @item]) do |f| %>
      <%= render 'form', item: f %>
    <% end %>

  </div>
</div>
