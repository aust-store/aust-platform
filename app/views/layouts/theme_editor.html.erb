<!DOCTYPE html>
<html>
<head>
  <title>Editor de tema</title>
  <%= stylesheet_link_tag "theme_editor/application", :media => "all" %>

  <script type="text/javascript">
    var env        = '<%= Rails.env %>',
        themeId    = '<%= @theme.id %>',
        previewUrl = '<%= admin_theme_preview_url(@theme) %>',
        currentTheme = <%= raw @theme.to_json(root: false, except: [:id, :company_id]) %>,
        images     = {
          'refresh_icon': '<%= asset_path("theme_editor/refresh_icon.png") %>'
        };
  </script>

  <%= javascript_include_tag "theme_editor/application" %>
  <%= airbrake_javascript_notifier %>
</head>
<body>

</body>
</html>
