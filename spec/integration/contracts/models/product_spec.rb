require 'integration_spec_helper'

describe "Product model contract" do
  subject {  }

  pending "responds to id" do
    expect do
      subject.new.id
    end.to_not raise_error
  end

  pending "responds to title" do
    expect do
      subject.new.title
    end.to_not raise_error
  end

  pending "responds to description" do
    expect do
      subject.new.description
    end.to_not raise_error
  end

  pending "responds to price" do
    expect do
      subject.new.price
    end.to_not raise_error
  end
end
